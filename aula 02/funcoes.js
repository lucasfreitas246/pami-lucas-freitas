/*function MultiplicaNumeros() {
    let n1 = 2;
    let n2 = 3;

    console.log(n1 * n2);
    // + (soma)
    // - subtração
    // * (multiplicação)
    // / (divisão)
    // ^ (potência)
    // % (sobra de divisão)
}

MultiplicaNumeros();

function MultiplicacaocomParametros(v1, v2) {
    console.log(v1,"x",v2,"=",v1*v2);
}

MultiplicacaocomParametros(2,5);
MultiplicacaocomParametros(10,15);
MultiplicacaocomParametros(200,500);

function EquacaodeBurrinho(x, y, z){
    let resultado = (x + y) / z;
    return resultado;                                                  }


console.log(EquacaodeBurrinho(2,3,5));*/

const EquacaodeTontinho = (x,y,z,w) => {
    return resultado = (x + y) * (z + w) / 2;
}
    
console.log("O resultado é: ", EquacaodeTontinho(3, 5, 8, 13));
